// "Are you sure?" + sad-face easter egg
export const EASTER_EGG_NAME = 'celal'; // matched case-insensitively, as a substring
export const CONFIRM_DELAY_MS = 1000; // delay after the result dialog opens before asking "are you sure?"
export const SAD_FACE_COUNT = 40; // how many sad-face popups spawn after confirming
export const SAD_FACE_STAGGER_MS = 100; // delay between each popup appearing
export const SAD_FACE_LIFETIME_MS = 10000; // each popup closes itself after this long
export const SAD_FACES = ['😢', '😭', '😞', '🥺', '😿', '💔'];
