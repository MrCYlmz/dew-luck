// Runaway "Select" button easter egg
export const RUNAWAY_NAME = 'celal'; // matched case-insensitively, as a substring
export const RUNAWAY_DURATION = 30000; // milliseconds the button keeps fleeing
export const RUNAWAY_TRIGGER_RADIUS = 110; // px — a cursor closer than this pushes the button
export const RUNAWAY_ESCAPE_PADDING = 40; // px extra distance it jumps beyond the radius
export const RUNAWAY_VIEWPORT_MARGIN = 12; // px — never leaves the viewport
export const RUNAWAY_TRANSITION_MS = 120; // transform transition duration
